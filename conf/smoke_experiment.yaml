# Smoke experiment configuration
defaults:
  - data: smoke_128
  - model/physical: smoke
  - model/synthetic: smoke_unet
  - trainer: synthetic
  - generation: default
  - evaluation: default
  - _self_

run_params:
  experiment_name: 'smoke_unet_128_v1'
  notes: 'Training UNet on Smoke 128x128'
  mode: ['train', 'evaluate']
  model_type: 'synthetic'

model:
  synthetic:
    model_save_name: 'smoke_unet_128'

trainer_params:
  train_sim: [0, 1, 2, 3, 4, 5, 6, 7, 8]
  epochs: 1000
  batch_size: 16
  num_predict_steps: 4
  use_sliding_window: true

evaluation_params:
  test_sim: [0, 9]
  num_frames: 51

project_root: ${hydra:runtime.cwd}
